export type PrimitiveUser = {
    name: string;
    email: string;
    password: string;
    phone: string;
}

export type User = {
    id: number;
    name: string;
    email: string;
    password: string;
    phone: string;
}

export type SessionUser = {
    id: number;
    name: string;
    email: string;
    phone: string;
}
